input {
    pipeline {
        address => app_pipeline 
    }
}


filter {
}

output {
    elasticsearch {
        index => "app-%{+YYYY.MM.dd}"
        hosts=> ["https://es01:9200", "https://es02:9200", "https://es03:9200"]
        user=> "${ELASTIC_USER}"
        password=> "${ELASTIC_PASSWORD}"
        cacert=> "certs/ca/ca.crt"
    }
}